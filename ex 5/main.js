// Escreva uma função que receba dois argumentos, largura e altura, e retorne true se estiver no modo paisagem

function Paisagem( largura, altura) {

    if (largura > altura) {
        return true;
    } else {
        false;
    }
}

console.log( Paisagem(1920, 1080))